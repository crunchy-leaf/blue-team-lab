# Test cases

This folder contains example payloads used to validate the WAF's SQL injection, XSS, command injection, and path traversal detection logic during development. As well as a payload designed to test whether rules can be bypassed.
